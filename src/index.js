import ReactDOM from 'react-dom/client'; // Import third party library  : react-dom and react together forms a library 

import './index.css'; 
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));  // Create main hook : render to html file to the user Interface

root.render(
  <App />
);